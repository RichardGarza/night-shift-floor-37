# Config drop-in

1. Merge `DefaultInput.ini` Enhanced Input class defaults into your project's `Config/DefaultInput.ini`.
2. Create IMC / IA assets per `../INPUT_MAPPING.md`.
3. Optionally set `GlobalDefaultGameMode=/Script/NightShiftFloor37.ArenaGameMode` in `DefaultEngine.ini` after the module builds.

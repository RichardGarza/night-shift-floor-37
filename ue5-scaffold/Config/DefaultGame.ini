; Night Shift — Floor 37 — DefaultGame.ini stub (Phase 5)
; Merge into the host UE5 project's Config/DefaultGame.ini after the module builds.

[/Script/EngineSettings.GeneralProjectSettings]
ProjectName=NightShiftFloor37
Description=Night Shift — Floor 37 arena shooter scaffold

[/Script/Engine.GameModeBase]
; C++ GameMode class used when GlobalDefaultGameMode / World Settings point here.
; AArenaGameMode sets DefaultPawnClass = ANightShiftCharacter and HUDWidgetClass = UHUDWidget in
; its constructor; a Blueprint subclass can override both.
; GameMode class path: /Script/NightShiftFloor37.ArenaGameMode

[/Script/NightShiftFloor37.ArenaGameMode]
; Optional: soft class defaults after module is compiled.
; GameConfig=/Game/Data/DA_GameConfig.DA_GameConfig
; HUDWidgetClass=/Game/UI/WBP_NightShiftHUD.WBP_NightShiftHUD_C
